     #include <stdio.h>
     #include <conio.h>
     #include <dos.h>
     #include<iostream.h>
      void main()
      {
      clrscr();
      int h,m,s;
      h=0;
      m=0;
      s=0;
      while(1)
      {
      if(s>59)
       {
       m=m+1;
       s=0;
       }
      if(m>59)
       {
       h=h+1;
	m=0;
	}
      if(h>11)
       {
       h=0;
	m=0;
	 s=0;
	  }
      delay(1000);
      s=s+1;
      cout<<"\n DIGITAL CLOCK";
      cout<<"\n HOUR:MINUTE:SECOND";
      cout<<"\n%d:%d:%d"<<h<<m<<s;
      }
      getch();
      }