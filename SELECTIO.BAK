#include<stdio.h>
#include<conio.h>
void main()
{
int n,i,j,max,a[100],elt;
printf("\nenter the no. of elements\n");
scanf("%d",&n);
for(i=0;i<n;i++)
scanf("%d",&a[i]);
for(i=0;i<n;i++)
printf(" %d",a[i]);
for(i=0;i<n;i++)
{
for(j=0;j<n;j++)
{
if(a[i]<a[j])
{elt=a[i];
a[i]=a[j];
a[j]=elt;}
}}
printf("\nthe sorted array is:");
for(i=0;i<n;i++)
printf("  %d",a[i]);
getch();
}